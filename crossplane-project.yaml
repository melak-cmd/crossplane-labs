apiVersion: dev.crossplane.io/v1alpha1
kind: Project
metadata:
  name: platform
spec:
  crossplane:
    version: '>=v2.3.0'
  skipDependencyResolution: true
  dependencies:
  - type: xpkg
    xpkg:
      apiVersion: pkg.crossplane.io/v1
      kind: Function
      package: ghcr.io/crossplane-contrib/function-go-templating
      version: '>=v0.12.0'
  - type: xpkg
    xpkg:
      apiVersion: pkg.crossplane.io/v1
      kind: Function
      package: ghcr.io/crossplane-contrib/function-auto-ready
      version: '>=v0.6.9'
  # - type: xpkg
  #   xpkg:
  #     apiVersion: pkg.crossplane.io/v1
  #     kind: Function
  #     package: xpkg.crossplane.io/crossplane-contrib/function-auto-ready
  #     version: '>=v0.6.9'
  description: |
    Platform configuration with app, network, and database compositions.
  license: Apache-2.0
  maintainer: melak-cmd
  readme: |
    Featuring Compositions for:
    - Apps (Deployment, HPA)
    - Networks (NetworkPolicy, Service, Ingress, DNS)
    - Databases (CloudNativePG Cluster, Secret)
  repository: registry.localhost:5000/platform
  source: github.com/melak-cmd/crossplane-labs
