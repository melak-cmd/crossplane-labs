## 1. Documentation

- [x] 1.1 Add `docs/compositions.md` with a per-Composition inventory table (Composition, XR, provider identity, downstream resource apiVersion/kind, conditional flags) covering `app-frontend`, `network-fullstack`, `database-cnpg`, and `database-backup-cnpg`; verify every row matches `apis/*/composition.yaml` and `functions/function-app/*.gotmpl`, and that the table output is reproducible via `python3 scripts/validate-rbac.py --render-doc-table` once the guard exists
- [x] 1.2 Add the RBAC grant matrix section stating the required verbs (get/list minimum, watch where declared) per downstream resource type and the provider identity that must hold them, citing the current wildcard grants at `providers/kubernetes.yaml:51-57` and `providers/helm.yaml:51-57` and noting provider-helm is currently unused by any Composition
- [x] 1.3 Add the ProviderConfig/identity contract section describing InjectedIdentity (provider SA acting on the same cluster) via ProviderConfig `default` (`providers/providerconfigs/default.yaml`) and noting that kubeconfig-backed ProviderConfigs shift the grant requirement to the remote cluster's identity
- [x] 1.4 Add the observation/staleness contract section covering: default poll cadence (10m with jitter; 30s while a composed Object is not Ready), `crossplane.io/poll-interval` override, the `crossplane.io/reconcile-requested-at` token acked in `status.lastHandledReconcileAt`, readiness gating of composed Objects against underlying resource status, and the alpha watch path (`--enable-watches` + `spec.watch: true`) with its misleading RBAC-denial symptom

## 2. Guard script and Makefile wiring

- [x] 2.1 Write `scripts/validate-rbac.py` deriving the downstream inventory from `apis/**/*composition*.yaml` (PyYAML parse of each `resources[].base` Object's `spec.forProvider.manifest.apiVersion/kind` and `providerConfigRef.name`) and from `functions/function-app/*.gotmpl` (regex on `forProvider.manifest` blocks); verify it prints the expected 8+ resources (Deployment, HPA, NetworkPolicy, Service x2, Ingress, Cluster, ScheduledBackup, Backup) and exits non-zero with an install hint when PyYAML is unavailable
- [x] 2.2 Implement the Kind→resource mapping table (deployments, horizontalpodautoscalers, networkpolicies, ingresses, services, secrets, clusters, scheduledbackups, backups), the per-(apiGroup,resource,verb) grant check against the provider ClusterRole rules (wildcard or specific match, get/list required, watch required only when declared), conditional-resource detection via `toFieldPath: spec.forProvider.manifest` patches, and ProviderConfig-reference file-existence checks (`providers/providerconfigs/{name}.yaml`); verify a hand-edited role missing e.g. `postgresql.cnpg.io/backups` get makes it exit 1 with that exact (group, resource, verb) listed
- [x] 2.3 Implement the doc-drift check parsing the inventory table from `docs/compositions.md` (warning by default, fatal with `--strict`) and the `--render-doc-table` mode; verify `--strict` fails when the doc table is edited out of sync and passes when reverted to the rendered output
- [x] 2.4 Add a `validate-rbac` target to the Makefile (`.PHONY` at Makefile:9-12, prerequisites of the `validate` umbrella at Makefile:125) running `python3 scripts/validate-rbac.py`; verify `make validate-rbac` exits 0 with the full pass table and that `make help` lists it

## 3. Verification

- [x] 3.1 Run `openspec validate --changes docs-composition-read-contract` (and `--strict`) and confirm the delta spec passes with a `## Purpose` at least 50 characters and `#### Scenario` headings
- [x] 3.2 Confirm the CNPG plural names in the mapping (`clusters`, `scheduledbackups`, `backups`) match the installed CRDs and that no unknown Kind triggers a failure on a clean run of `make validate-rbac`